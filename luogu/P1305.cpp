#include<bits/stdc++.h>
using namespace std;

struct node{
    node *left;
    node *right;
    char data;
};

node *root,*p;
int n;

node *find(char a){

}

void insert(char a,char b,char c){
    p=find(a);
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>n;
    for(int i=0;i<n;i++){
        string s;
        cin>>s;
        for(char j:s){
            
        }
    }
    return 0;
}